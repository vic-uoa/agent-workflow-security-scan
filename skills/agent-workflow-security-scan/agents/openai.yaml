interface:
  display_name: "Agent Workflow Security Gate"
  short_description: "Enterprise Dify DSL security gate"
  default_prompt: "Use $agent-workflow-security-scan to confirm one DSL and seed inputs, then report risks by responsible node and control domain with rules and paths retained as evidence."
